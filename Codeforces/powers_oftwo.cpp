#include <stdio.h>
#include <bits/stdc++.h>
#include <string>
#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	int n = 0;
	int k = 0;
	cin >> n >> k;
	if(n == k)
	{
		cout << "YES" << endl;
		for (int i = 0; i < n; ++i)
		{
			cout << n;
		}
	}
	else if(k == 1)
	{
		if( != )
	}
	int b = log2 (5);
	cout << b << endl;
}